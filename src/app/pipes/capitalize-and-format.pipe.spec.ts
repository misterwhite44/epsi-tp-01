import { CapitalizeAndFormatPipe } from './capitalize-and-format.pipe';

describe('CapitalizeAndFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalizeAndFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
